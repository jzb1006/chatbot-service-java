server:
  port: 8766
spring:
  threads:
    virtual:
      enabled: true
chatbot:
  device:
    config-path: /app/data/llm_config.json
  hermes:
    base-url: http://hermes:8642/v1
  voice:
    default-voice-id: default
